# Full dependencies for Week 3 (GPU-accelerated exercises)
# Includes everything from requirements.txt + GPU/torch support
# Install with: pip install -r requirements-cuda.txt --extra-index-url https://download.pytorch.org/whl/cu121

# API Clients
openai>=1.0.0
anthropic>=0.25.0
google-generativeai>=0.3.0

# LLM Orchestration & Tooling
langchain>=0.1.0
langchain-community>=0.0.1
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0

# GPU & Deep Learning (CUDA 12.1 compatible)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Transformers & Diffusion
transformers==4.46.0
diffusers==0.31.0
datasets>=3.6.0
huggingface-hub>=0.24.0

# Audio & ML Utilities
soundfile>=0.12.1
sentencepiece>=0.2.0
accelerate>=1.13.0
safetensors>=0.4.0
peft>=0.17.0
bitsandbytes>=0.49.0  # 4-bit / 8-bit quantization (Exercise 3.4)
